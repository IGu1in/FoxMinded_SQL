USE studentdb;

SELECT COURSES.NAME AS CoursesName, STUDENTS.FIRST_NAME + ' ' + STUDENTS.LAST_NAME AS StudentsName
FROM COURSES
JOIN GROUPS ON COURSES.COURSE_ID = GROUPS.COURSE_ID
JOIN STUDENTS ON GROUPS.GROUP_ID = STUDENTS.GROUP_ID 