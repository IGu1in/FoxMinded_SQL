USE studentdb;

SELECT GROUPS.NAME AS NAME, COUNT(STUDENTS.STUDENT_ID) AS StudentsCount
FROM GROUPS, STUDENTS
WHERE GROUPS.GROUP_ID = STUDENTS.GROUP_ID
GROUP BY NAME
HAVING COUNT(STUDENTS.STUDENT_ID) < 10